name: Release Please

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      release_as:
        description: Optional version override, for example 2.1.0
        required: false
        type: string

permissions:
  contents: read

concurrency:
  group: release-please-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release-please:
    name: Create or update release
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Create GitHub App token
        id: app_token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ vars.RELEASE_PLEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_PLEASE_PRIVATE_KEY }}
          permission-contents: write
          permission-issues: write
          permission-pull-requests: write

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"

      - name: Create release for merged release pull request
        shell: bash
        env:
          RELEASE_PLEASE_TOKEN: ${{ steps.app_token.outputs.token }}
        run: |
          set -euo pipefail

          npx --yes release-please@17.6.0 github-release \
            --token="${RELEASE_PLEASE_TOKEN}" \
            --repo-url="${GITHUB_REPOSITORY}" \
            --target-branch=main \
            --config-file=release-please-config.json \
            --manifest-file=.release-please-manifest.json

      - name: Determine version override
        id: release_version
        shell: bash
        env:
          GH_TOKEN: ${{ steps.app_token.outputs.token }}
          REQUESTED_RELEASE: ${{ inputs.release_as }}
        run: |
          set -euo pipefail

          release_as="${REQUESTED_RELEASE}"
          if [[ -z "${release_as}" ]]; then
            workspace_version="$(
              cargo metadata --no-deps --format-version 1 |
                jq -er '[.packages[].version] | unique | if length == 1 then .[0] else error("workspace versions differ") end'
            )"
            if ! gh api \
              "repos/${GITHUB_REPOSITORY}/git/ref/tags/v${workspace_version}" \
              >/dev/null 2>&1; then
              release_as="${workspace_version}"
            fi
          fi
          echo "release_as=${release_as}" >> "${GITHUB_OUTPUT}"

      - name: Create or update next release pull request
        shell: bash
        env:
          RELEASE_AS: ${{ steps.release_version.outputs.release_as }}
          RELEASE_PLEASE_TOKEN: ${{ steps.app_token.outputs.token }}
        run: |
          set -euo pipefail

          args=(
            release-pr
            --token="${RELEASE_PLEASE_TOKEN}"
            --repo-url="${GITHUB_REPOSITORY}"
            --target-branch=main
            --config-file=release-please-config.json
            --manifest-file=.release-please-manifest.json
          )
          if [[ -n "${RELEASE_AS}" ]]; then
            args+=(--release-as="${RELEASE_AS}")
          fi

          npx --yes release-please@17.6.0 "${args[@]}"
